import React from 'react';
import ListTable from './ListTable';

const Dashboard = (props) => {
    return (
        <div>
            <ListTable />
        </div>
    );
};

export default Dashboard;