./node_modules

# dependencies
/client/node_modules
/server/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
/server/.env

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# For any type of mongoose server selection error on the mongoose.config file, add a "family: 4" to it
# See below for example of the mongoose.config file
# mongoose.connect("mongodb://localhost:27017/playerManager", {
#     useNewUrlParser: true,
#     useUnifiedTopology: true,
#     family: 4, //Without this I keep getting the mongoose server selection error
# })
# .then(() => {console.log("Established connection to the db, from mongoose.config")})
# .catch((err) => {console.log(err, "from mongoose.config")});

# see here for a website reference where I found the solution: https://you.com/search?q=const%20serverselectionerror%20%3D%20new%20serverselectionerror%28%29%3B%20%5E%20mongooseserverselectionerror%3A%20connect%20econnrefused%20%3A%3A1%3A27017


#Incorporate the following technologies:
#Docker
#GraphQL
#Redux
#Docker Compose

#What I didnt get to:
#I didnt finish the login and logout functionality
#I also was not able to finish the functionality of the buttons under the "Actions" column